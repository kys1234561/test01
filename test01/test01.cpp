// test01.cpp: 定义应用程序的入口点。
//
/*
sadassadada
dsadasdsad
*/
#include "test01.h"//引用头文件。类似python的import，
//此处是自己写的库，所以用""来导入。

using namespace std;//命名空间，后面课程会讲，类似python的模块

int main()//程序入口，开始执行的地方
{
	cout << "Hello CMake。" << endl;
	cout << "Hello CMake。" << endl;
	cout << "Hello CMake。" << endl;
	system("pause");
	return 0;
}
